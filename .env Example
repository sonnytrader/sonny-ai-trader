PORT=3000

# JWT ve şifreleme
JWT_SECRET=your_jwt_secret_here
ENC_KEY=32_bytes_key____________________
ENC_IV=16_bytes_iv______

# Varsayılan yok; kullanıcılar kendi anahtarlarını ekleyecek
# Bitget keys kullanıcı bazlı DB'de saklanır (api_keys)

# Opsiyonel defaults (kullanıcı config override eder)
# MIN_CONF_AUTO=60
